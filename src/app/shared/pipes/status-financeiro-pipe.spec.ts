import { StatusFinanceiroPipe } from './status-financeiro-pipe';

describe('StatusFinanceiroPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusFinanceiroPipe();
    expect(pipe).toBeTruthy();
  });
});
